3. Configurar tu servidor
📱 AI Contestadora — App Móvil
App en React Native (Expo) que se conecta con el servidor AI Contestadora.
Qué hace
Muestra el historial de llamadas filtradas por la IA
Sincroniza tus contactos con el servidor automáticamente
Te permite llamar o enviar WhatsApp desde el detalle de cada llamada
Configuración del servidor, filtros y mensaje de la IA
Instalación rápida
1. Instalar Node.js y Expo CLI
# Instalar Node.js desde https://nodejs.org (versión 18 o superior)

# Instalar Expo CLI globalmente
npm install -g expo-cli eas-cli
2. Instalar dependencias
cd contestadora-app
npm install